# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository

This is a practice/experimental repository (`claude-practice`). It is currently empty aside from Claude Code configuration.

## Configuration

- `.claude/settings.local.json` — local Claude Code permissions (allowed Bash commands, etc.)
